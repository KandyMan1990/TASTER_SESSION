using UnityEngine;

public class Page : MonoBehaviour
{
    //public variables are viewable and editable in the Inspector
    #region Public Variables
    //variable to hold the name of the page such as "Variable"
    public string Name;

    //variable to hold the message that will be displayed on the page
    public string Message;

    //variable to hold the example message that will be shown on the page
    public string Example;
    #endregion
}